#!/usr/bin/local/perl

# I'm just rewriting this from scratch so it can do all the things at once

use Lingua::Ispell qw( spellcheck ); # import the function
use Data::Dumper;


# import the password file into memory

# keep just the passwords that match a spellcheck

# process those passwords a little bit, store in a hash

# run through the target file for subsitution

# print the output
